<!DOCTYPE html>
<html>
<head>
    <title>Matches</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container py-5">

   <h2 class="mb-4">
    Top Matches for <%= startup.startupName %>
</h2>

    <% if (matches.length === 0) { %>
        <p>No investors found.</p>
    <% } %>

    <% matches.forEach(match => { %>

        <div class="card p-3 mb-3 shadow-sm">
            <h5><%= match.investor.name %></h5>
            <p>Preferred Industry: <%= match.investor.preferredIndustry %></p>
            <p>Preferred Stage: <%= match.investor.preferredStage %></p>
            <p>Max Investment: ₹<%= match.investor.maxInvestment %></p>

            <p>
                Match Score:
                <strong class="text-success">
                    <%= match.score %>%
                </strong>
            </p>
        </div>

    <% }) %>

</div>

</body>
</html>