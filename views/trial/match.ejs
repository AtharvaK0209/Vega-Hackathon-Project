<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Investment Matches | Nexus</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
        .match-card { border-left: 5px solid #0d6efd; transition: transform 0.2s; }
        .match-card:hover { transform: translateY(-5px); }
        .score-circle { width: 60px; height: 60px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-weight: bold; font-size: 1.1rem; }
    </style>
</head>
<body class="bg-light">

<div class="container py-5">
    <div class="d-flex justify-content-between align-items-center mb-5">
        <div>
            <h2 class="fw-bold text-dark">Investment Matches</h2>
            <p class="text-muted">AI-powered recommendations for <strong><%= startup.startupName %></strong></p>
        </div>
        <a href="/startup/dashboard" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left"></i> Back to Dashboard
        </a>
    </div>

    <% if (matches.length === 0) { %>
        <div class="text-center py-5">
            <i class="bi bi-search" style="font-size: 3rem; color: #ccc;"></i>
            <p class="mt-3 text-muted">No investors found in our database yet.</p>
        </div>
    <% } %>

    <div class="row">
        <% matches.forEach(match => { %>
            <div class="col-12 mb-4">
                <div class="card match-card shadow-sm">
                    <div class="card-body p-4">
                        <div class="row align-items-center">
                            
                            <div class="col-md-7">
                                <h4 class="fw-bold mb-1"><%= match.investor.investorName %></h4>
                                <p class="text-primary mb-2">
                                    <i class="bi bi-building"></i> <%= match.investor.firmName || 'Independent Investor' %>
                                </p>
                                
                                <div class="mb-3">
                                    <% match.investor.preferredIndustries.forEach(industry => { %>
                                        <span class="badge bg-light text-dark border"><%= industry %></span>
                                    <% }) %>
                                </div>

                                <div class="bg-light p-3 rounded-3 border-start border-info border-4">
                                    <small class="text-uppercase fw-bold text-info d-block mb-1">
                                        <i class="bi bi-robot"></i> Gemini Insight
                                    </small>
                                    <p class="mb-0 small text-dark"><%= match.reasoning %></p>
                                </div>
                            </div>

                            <div class="col-md-5 mt-3 mt-md-0 border-start ps-md-4">
                                <div class="d-flex align-items-center mb-3">
                                    <div class="score-circle <%= match.score >= 80 ? 'bg-success text-white' : 'bg-warning text-dark' %> me-3">
                                        <%= match.score %>%
                                    </div>
                                    <div>
                                        <h6 class="mb-0 fw-bold">Compatibility Score</h6>
                                        <small class="text-muted">Based on industry, stage & budget</small>
                                    </div>
                                </div>

                                <div class="row g-2 small mb-3">
                                    <div class="col-6">
                                        <strong>Stage:</strong> <%= match.investor.preferredStage %>
                                    </div>
                                    <div class="col-6">
                                        <strong>Location:</strong> <%= match.investor.locationPreference %>
                                    </div>
                                    <div class="col-12">
                                        <strong>Range:</strong> ₹<%= match.investor.minInvestment.toLocaleString() %> - ₹<%= match.investor.maxInvestment.toLocaleString() %>
                                    </div>
                                </div>

                                <button class="btn btn-primary w-100">
                                    <i class="bi bi-envelope"></i> Send Pitch Deck
                                </button>
                            </div>

                        </div>
                    </div>
                </div>
            </div>
        <% }) %>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>